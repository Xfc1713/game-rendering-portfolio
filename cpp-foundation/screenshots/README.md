# cpp-foundation 运行截图

请将截图放入本目录，建议文件名：

| 文件 | 内容 |
|------|------|
| `run-success.png` | 终端显示 Lambert N·L、RAII、完成提示 |
| `run-success-2.png` | （可选）第二张不同角度 |

放好后在项目根目录执行：

```cmd
git add cpp-foundation/screenshots/
git commit -m "docs: 添加 cpp-foundation 运行截图"
git push
```

GitHub 上即可在 README 中直接显示。
